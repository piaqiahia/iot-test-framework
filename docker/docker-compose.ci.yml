services:
  redis:
    ports:
      - "6379:6379"
  postgres:
    ports:
      - "5432:5432"
    volumes:
      - ./init_db.sql:/docker-entrypoint-initdb.d/init_db.sql
  jetlinks:
    ports:
      - "8848:8848"
      - "1885:1885"   # MQTT Broker 端口